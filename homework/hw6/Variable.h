#ifndef UNTITLED_VARIABLE_H
#define UNTITLED_VARIABLE_H


class Variable {

    // suggestions:

    // string name;
    // string type;

    // virtual Variable add(Variable) = 0;
    // virtual Variable subtract(Variable) = 0;
    // virtual Variable multiply(Variable) = 0;
    // virtual Variable divide(Variable) = 0;
    // virtual Variable lessThan(Variable) = 0;
    // ...

};


#endif //UNTITLED_VARIABLE_H
